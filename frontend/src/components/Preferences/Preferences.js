import React from 'react';
import '../../main.css'

export default function Preferences() {
    return (
        <div className='page'>
            <h2>Preferences</h2>
            <button className='btn danger'>TEST</button>
        </div>

    );
}